# SmartStay
SmartStay is a Java-based hotel reservation system that manages room bookings efficiently. It uses JDBC to connect with a MySQL database for storing reservation details. The project demonstrates Java packages, database operations, and exception handling to build a simple and reliable booking system.
